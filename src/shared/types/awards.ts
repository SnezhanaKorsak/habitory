export type TimerHabitResult = {
  timeResults: { [key: string]: number };
  timerGoal: number;
};

export type NumericHabitResults = {
  numericResults: { [key: string]: number };
  numericGoal: number;
};
