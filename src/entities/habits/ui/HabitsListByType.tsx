import { StyleSheet, View } from 'react-native';

import { HabitType } from '../../../shared/types/habit';

type Props = {
  type: HabitType;
};

export const HabitsListByType = ({}: Props) => {
  return <View></View>;
};
