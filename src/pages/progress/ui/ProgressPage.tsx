import { ProgressInfo } from '../../../entities/progress';
import { Layout } from '../../../widgets';

export const ProgressPage = () => {
  return (
    <Layout>
      <ProgressInfo />
    </Layout>
  );
};
